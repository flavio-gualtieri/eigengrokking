from __future__ import annotations

from configs.config import ExperimentConfig

EXPERIMENTS = [
    ExperimentConfig(
        dataset='MNIST',
        optimization_steps=5_000,
        train_points=1000,
        batch_size=64,
        weight_decay=0.01,
        initialization_scale=8,
        run_spectral=True,
        test_mode=True,
    ),
"""     ExperimentConfig(
        dataset='MNIST',
        optimization_steps=200_000,
        train_points=1000,
        batch_size=64,
        weight_decay=0.01,
        initialization_scale=2,
        run_spectral=True,
        test_mode=True,
    ),
    ExperimentConfig(
        dataset='MNIST',
        optimization_steps=200_000,
        train_points=1000,
        batch_size=64,
        weight_decay=0.01,
        initialization_scale=3,
        run_spectral=True,
        test_mode=True,
    ),
    ExperimentConfig(
        dataset='MNIST',
        optimization_steps=200_000,
        train_points=1000,
        batch_size=64,
        weight_decay=0.01,
        initialization_scale=4,
        run_spectral=True,
        test_mode=True,
    ),
    ExperimentConfig(
        dataset='MNIST',
        optimization_steps=200_000,
        train_points=1000,
        batch_size=64,
        weight_decay=0.01,
        initialization_scale=5,
        run_spectral=True,
        test_mode=True,
    ),
    ExperimentConfig(
        dataset='MNIST',
        optimization_steps=200_000,
        train_points=1000,
        batch_size=64,
        weight_decay=0.01,
        initialization_scale=6,
        run_spectral=True,
        test_mode=True,
    ),
    ExperimentConfig(
        dataset='MNIST',
        optimization_steps=200_000,
        train_points=1000,
        batch_size=64,
        weight_decay=0.01,
        initialization_scale=7,
        run_spectral=True,
        test_mode=True,
    ),
    ExperimentConfig(
        dataset='MNIST',
        optimization_steps=200_000,
        train_points=1000,
        batch_size=64,
        weight_decay=0.01,
        initialization_scale=8,
        run_spectral=True,
        test_mode=True,
    ),
    ExperimentConfig(
        dataset='MNIST',
        optimization_steps=200_000,
        train_points=1000,
        batch_size=64,
        weight_decay=0.01,
        initialization_scale=9,
        run_spectral=True,
        test_mode=True,
    ),
    ExperimentConfig(
        dataset='MNIST',
        optimization_steps=200_000,
        train_points=1000,
        batch_size=64,
        weight_decay=0.01,
        initialization_scale=10,
        run_spectral=True,
        test_mode=True,
    ),
    ExperimentConfig(
        dataset='MNIST',
        optimization_steps=200_000,
        train_points=1000,
        batch_size=64,
        weight_decay=0.01,
        initialization_scale=11,
        run_spectral=True,
        test_mode=True,
    ), """
]
