# Gate figure summary: p91_wd0.6_seed0

Generated by `python -m analysis.make_figures reports/spectra/MODULAR_p91_wd=0.6_seed=0.parquet results/MODULAR/no_spectrum/modulus=91/depth=1/width=128/init=1/wd=0.6/seed=0`.

## Onset

- 2x-chance crossing: step 3000
- Logistic-fit landmark (exp(mu - 2 sigma)): step 4682.9
- Agree within 1.2x: False -- **inspect the accuracy curve before trusting the fit value.**

## Sanity checks

- Pre-onset log-residual (trace_rel vs. ||theta||^-2 prediction), all 66 checkpoints: mean -0.505, sd 0.876, range 4.446
- Smoothness (mean |d(log)| per step), all checkpoints: trace_rel=0.5945, trace_corr_rel=0.5508
- Same two checks with the 5 checkpoint(s) below train_acc=0.9 dropped (61 remain): pre-onset log-residual mean -0.297, sd 0.428, range 1.535; smoothness trace_rel=0.3962, trace_corr_rel=0.3463.

## Headline

Across steps 500-3000 the corrected trace changes by -7.7% (norm-corrected, i.e. within noise of the ||theta||^-2 prediction); from step 3000 the corrected trace and the raw prediction diverge, with corrected trace moving -41.5% while top_eig_corr rises 2.7x and effective_rank falls 13.4x.

## Limitations

**The qualitative conclusion above did not survive both alternative normalizers** (see `*_robustness.png`) -- treat the pre-onset window as inconclusive rather than as evidence either way. The ||theta||^-2 correction is a heuristic, not validated against an independent normalizer for this run.

`bulk_edge`/`conditioning` are excluded from this figure -- the weighted median sits inside the near-zero degenerate spike and the sign is noise-determined; see "Demoted from headline figures/claims" in `reports/spectral_validation.md`.
