# Gate figure summary: p91_wd1_seed0

Generated by `python -m analysis.make_figures reports/spectra/MODULAR_p91_wd=1_seed=0.parquet results/MODULAR/no_spectrum/modulus=91/depth=1/width=128/init=1/wd=1/seed=0`.

## Onset

- 2x-chance crossing: step 2400
- Logistic-fit landmark (exp(mu - 2 sigma)): step 2924.2
- Agree within 1.2x: False -- **inspect the accuracy curve before trusting the fit value.**

## Sanity checks

- Pre-onset log-residual (trace_rel vs. ||theta||^-2 prediction), all 150 checkpoints: mean -1.025, sd 1.056, range 5.022
- Smoothness (mean |d(log)| per step), all checkpoints: trace_rel=0.8665, trace_corr_rel=0.8030
- Same two checks with the 19 checkpoint(s) below train_acc=0.9 dropped (131 remain): pre-onset log-residual mean -0.819, sd 0.546, range 2.004; smoothness trace_rel=0.6800, trace_corr_rel=0.5989.

## Headline

corrected trace moves -83.0% before the onset step (2400), a lead-time candidate -- ahead of the accuracy transition rather than concurrent with it. From onset to the end of this window it moves a further +205.8%, alongside top_eig_corr rising 16.1x and effective_rank falling 14.2x.

## Limitations

**The qualitative conclusion above did not survive both alternative normalizers** (see `*_robustness.png`) -- treat the pre-onset window as inconclusive rather than as evidence either way. The ||theta||^-2 correction is a heuristic, not validated against an independent normalizer for this run.

`bulk_edge`/`conditioning` are excluded from this figure -- the weighted median sits inside the near-zero degenerate spike and the sign is noise-determined; see "Demoted from headline figures/claims" in `reports/spectral_validation.md`.
